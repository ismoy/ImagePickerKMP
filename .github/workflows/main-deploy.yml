# =============================================================================
# Main Branch Deployment — DISABLED
# The project is being restructured from a single "library" module into
# multiple sub-modules (imagepicker-core, imagepickerkmp-photo, etc.).
# This workflow will be re-enabled once the new multi-module publish
# configuration is finalized.
# =============================================================================
name: Main Branch Deployment

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: false  # ← disabled until multi-module publish is configured
    steps:
      - name: Placeholder
        run: echo "Main deploy workflow is disabled during modularization"